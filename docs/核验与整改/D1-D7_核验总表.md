# D1-D7 真实完整性核验总表

> 审计日期：2026-08-29；工作目录：`E:\engineering\contest\2026duthackthon\CollabLedger`；分支：`dev_D`；HEAD：`926b4c33c7be732b7a543ad34caea326c7151982`。
> 本轮只做观察、诊断和文档写入，不做功能代码整改，不 push、merge、改 CI、迁移数据库或远程写操作。

## 1. 基线与方法

- `git fetch origin --prune` 已执行；工作区初始干净；`origin/main=2b9d97c...` 是 `dev_D=926b4c3...` 的祖先，且未改写 dev_D 历史。
- GitNexus 原增量分析遇到 `file_fts` 不一致；只读修复 FTS 后重新建立索引成功：2,143 nodes、4,892 edges、70 clusters、184 flows。该修复只更新被忽略的 `.gitnexus/` 索引，不进入提交。
- 亲自完整阅读：仓库 AGENTS.md；D2-D7 深化方案；产品路线图；团队分工与任务拆解；项目书；API接口契约；B业务接口；评审人与导师对接；BACKEND_ARCHITECTURE、DATABASE_SCHEMA、DEPLOYMENT、BACKUP_RESTORE、PERMISSION_MODEL、SECURITY、演示手册；并抽查所有 D 责任代码、路由、模型、前端和测试。
- 证据分层：代码/调用链，数据库 schema，接口 smoke，测试输出，前端 build/static review，真实服务与重启；“测试通过”不等于“真实外部主路径”或“产品完整”。

## 2. 总体判定

| 阶段 | 代码存在 | 测试通过 | 真实运行验证 | 产品完整 | 总结 |
|---|---|---|---|---|---|
| D1 | 是 | 是 | 规则路径/SQLite 是；LLM/浏览器否 | 部分 | 核心推荐链路扎实，D6授权、采纳原子性、真实 LLM/浏览器未闭环 |
| D2 | 是 | 是 | SQLite规则路径是；LLM/PG否 | 部分 | 加权与排序存在，但 unfinished、加权风险、错误可观测性有缺陷 |
| D3 | 是 | 是 | SQLite落库/重启是；LLM/PG否 | 部分 | 周报持久化成立，贡献/工时口径与 PDF 假成功需整改 |
| D4 | 是 | 是 | SQLite fallback/重启记忆是；LLM成功否 | 部分 | ReAct/摘要/白名单存在，但工具写库、事实约束和引用可追溯不足 |
| D5 | 是 | 是（18 个专项测试） | 本地持久化/Webhook签名链路是；外网三平台否 | 部分（外部阻塞） | 统一平台、Fernet、state、GitHub四类事件/Webhook/反写、飞书/腾讯适配已落地；凭据与容器实测待外部资源/D7 |
| D6 | 部分 | 是 | SQLite画像是；授权/浏览器否 | 缺失/部分 | 画像聚合与推荐兜底存在，授权/合作关系/长期推荐缺失 |
| D7 | 部分 | 是 | 本地服务/SQLite是；Docker/PG/浏览器/外部否 | 缺失/部分 | 81+8、build、CI静态文件和手册存在，但无完整端到端验收 |

## 3. 关键阻塞

### P0/P1 产品与安全

1. D5 安全基线已整改；当前关键阻塞转为外部平台凭据、公网 Webhook 回调和 GitHub diff 增删行统计。
2. D6 跨项目授权 API 缺失，画像读取以历史共同 membership 为依据，完整 PRD 隐私边界未成立。
3. D4 Agent 最终答案只受 prompt 约束，没有事实一致性校验；weekly_report 工具会写库，违反只读工具边界。
4. D3 周报把 pending/disputed 贡献计入总数，任务工时与打卡工时可能双计；PDF endpoint 是单行占位且中文损坏。
5. D5 compose 尚未透传 GitHub/飞书/腾讯文档/Webhook 环境变量，留 D7 部署整改。
6. 无 Playwright；没有桌面/390px 全链路证据。

### 外部阻塞

真实 GitHub OAuth/Webhook/反向写入、飞书、腾讯文档、PostgreSQL、Docker daemon、真实域名/HTTPS/证书仍需要外部资源或服务。真实 LLM 主路径已在 D2 验证成功。本轮只检查外部平台变量是否配置，未读取/输出任何密钥。

## 4. 建议严格整改顺序

1. **D1**：确认 reviewer 联动；修复推荐采纳事务/幂等；纳入 D6 授权边界。
2. **D2**：冻结 unfinished、加权风险和 critical 重复口径；修错误吞异常和前端静默降级。
3. **D3**：冻结贡献/工时统计；移除或真实实现 PDF；确认首次生成是否允许 Agent/工作区自动触发。
4. **D4**：严格只读工具；增加 fact/citation policy、prompt injection 边界、会话归属和错误可观测性。
5. **D5**：统一适配层、安全凭证/state、GitHub 增量/重试/绑定/Webhook/反写、飞书/腾讯文档代码链路已完成；外部凭据实测与 compose 透传留 D7。
6. **D6**：先授权模型，再补画像契约、合作关系、长期推荐和前端授权/来源页。
7. **D7**：Playwright→SQLite兼容/重启→PG/Alembic→Docker/备份→真实 LLM/外平台→演示复验。

## 5. 需要用户确认的设计分支

1. D2：同一高优先级未分配任务是否保留 `unassigned_task + critical_unassigned` 两条，还是聚合为一条专项风险？
2. D3：周报贡献是否只统计 confirmed；任务 actual_hours 与 checkin hours 采用“二选一”还是分项展示？
3. D4：Agent 会话是项目共享还是按用户隔离；Agent 是否允许首次生成并落库周报？
4. D1/D6：历史画像参与推荐的授权粒度（按用户全局、按项目、按用途）和撤销后的历史保留策略。
5. D1/B：推荐采纳或手选是否联动 reviewer/mentor 展示或设置；当前代码不自动改变 reviewer。
6. D5：已确认包含 Webhook 与 GitHub 反向写入，且只允许用户显式操作；设计分支已关闭。
7. D6/API：采用深化方案 profile 字段，还是兼容 API 契约中的 `completed_task_count/on_time_rate/top_skills/data_sources` 扩展字段。

## 6. 本轮验证命令与结果

```text
uv run --no-project --with-requirements requirements.txt --with pytest pytest backend/test -q --tb=short
=> 81 passed, 1 warning, 216.83s

cd frontend; npm run test -- --run
=> 3 test files passed, 8 tests passed

cd frontend; npm run build
=> typecheck passed; vite build passed

python -m compileall -q backend alembic
=> passed
```

本地临时服务 smoke：`/api/health` ok；登录 ok；D1 rule recommendation、D2 risks、D3 weekly first/second read、D4 fallback/tool trace/citations ok；停止后重新启动同库，D3 history 和 D4 sessions 保留。临时数据库在系统 Temp 目录，不属于仓库交付物。

## 7. 结论

当前 dev_D 是“核心 AI/分析代码已形成、质量门禁静态补齐、外部和长期协作尚未完整”的阶段性实现，不是满足完整 PRD 的 D1-D7 完成交付。整改必须在用户确认上述设计分支后，按 D1→D7 串行推进；本轮到此暂停。